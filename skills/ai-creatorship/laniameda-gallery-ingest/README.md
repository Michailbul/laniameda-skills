# laniameda-gallery-ingest

> ⚠️ This is a reference copy. The most up-to-date version lives in the laniameda.gallery GitHub repository.

**Canonical source:** https://github.com/Michailbul/laniameda-gallery/tree/main/skills/laniameda-gallery-ingest

When the gallery ingest contract changes (schema, validators, API), the skill is updated there first.
To refresh this copy and all agent workspaces:

```bash
cd ~/work/laniameda/laniameda.gallery && git pull && bun run skills:update
```
